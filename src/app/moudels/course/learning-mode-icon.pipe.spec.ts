import { LearningModeIconPipe } from './learning-mode-icon.pipe';

describe('LearningModeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningModeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
